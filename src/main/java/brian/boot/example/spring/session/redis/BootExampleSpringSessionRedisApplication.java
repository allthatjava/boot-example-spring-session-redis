package brian.boot.example.spring.session.redis;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class BootExampleSpringSessionRedisApplication {

    public static void main(String[] args) {
        SpringApplication.run(BootExampleSpringSessionRedisApplication.class, args);
    }

}
